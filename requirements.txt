# Hyperliquid Trading Bot Dependencies

# Hyperliquid SDK (for authenticated trading)
hyperliquid-python-sdk>=0.8.0

# Ethereum account management (for wallet signing)
eth-account>=0.10.0

# Environment variable management
python-dotenv>=1.0.0

# HTTP requests (for public API calls)
requests>=2.31.0

# Async HTTP client (for local AI server communication)
httpx>=0.27.0

# WebSocket streaming (for real-time data)
websockets>=12.0

# Terminal UI (retro dark theme dashboard)
textual>=0.47.0
textual-slider>=0.1.0
textual-plotext>=0.2.0

# Interactive CLI prompts (arrow key navigation)
questionary>=2.0.0

# Development tools
watchfiles>=0.21.0  # File watcher for hot reload

# Volume Profile - Historical Trade Data
pyarrow>=14.0.0  # Parquet file support for trade data storage
lz4>=4.3.0  # LZ4 decompression for Hyperliquid S3 data
